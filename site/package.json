{
  "name": "yondermesh-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "yondermesh documentation site (VitePress)",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "sync": "node ../scripts/docs/sync-all.mjs",
    "check-drift": "node ../scripts/docs/check-drift.mjs",
    "verify": "node ../scripts/docs/verify-links.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}

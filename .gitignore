# 内部文档（不可开源，禁止提交）
# 含架构设计、实施计划、验收基线等内部信息
# 只忽略顶层 docs/，不影响 scripts/docs/ 等子目录
/docs/

# 内部规范文档（SDD / spec，仅本地用，禁止提交）
/specs/

# 运行时状态（daemon 当前会话快照等，禁止提交）
.ymesh-runtime/

# 内部输出产物（PPT / 提取消息等，禁止提交）
/outputs/

# 一次性内部脚本（如 BP 提取脚本，禁止提交）
scripts/extract_bp_sessions.py

# 外部参考项目（仅本地学习用，禁止提交）
examples/

# 依赖
node_modules/

# VitePress 构建产物
site/.vitepress/dist/
site/.vitepress/cache/
site/node_modules/

# 构建产物
dist/

# 本地配置与数据
*.local
.env
.env.*
!.env.example

# SQLite 数据库
*.db
*.db-journal
*.db-wal
*.db-shm

# 密钥文件
*.pem
*.key

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# 系统文件
.DS_Store
Thumbs.db

# 日志
*.log
logs/

# 测试覆盖率
coverage/

# 临时文件
tmp/
temp/
.aider*
